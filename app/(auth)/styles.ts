import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  screen: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    justifyContent: 'center',
    alignItems: 'center'
  },
  form: {
    padding: 18,
    display: "flex",
    gap: 8,
    width: "100%"
  },
  signUpText: {
    fontWeight: "bold"
  }
});